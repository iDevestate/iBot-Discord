const Discord = require("discord.js");

module.exports.run = async(bot, message, args) => {
   message.reply("Trigon can be downloaded from http://arponag.xyz/");
}

module.exports.help = {
    name: "trigon"
}